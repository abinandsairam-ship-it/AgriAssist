
{
  "entities": {
    "Entry": {
      "title": "Entry",
      "description": "Represents a user-submitted form entry in Agriassist, stored in Firestore.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "User ID of the user who submitted the form. Empty string if user is not authenticated."
        },
        "timestamp": {
          "type": "number",
          "description": "Timestamp of when the form was submitted."
        },
        "cropType": {
          "type": "string",
          "description": "The predicted crop type from the AI model."
        },
        "condition": {
          "type": "string",
          "description": "The predicted health status or disease condition of the crop."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the uploaded crop image stored in Cloud Storage.",
          "format": "uri"
        },
        "confidence": {
          "type": "number",
          "description": "The prediction confidence level of the AI model (0-1)."
        }
      },
      "required": [
        "userId",
        "timestamp",
        "cropType",
        "condition",
        "imageUrl",
        "confidence"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/crop_data/{entryId}": {
      "schema": {
        "$ref": "#/entities/Entry"
      },
      "description": "Stores user-submitted form entries. Includes fields for userId, timestamp, cropType, condition, imageUrl, and predictionConfidence."
    }
  }
}
